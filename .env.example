# Main database (used when running API via docker-compose)
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/medspa_db

# For running tests: use test DB. When using docker-compose exec api pytest,
# ensure test DB is running (docker-compose -f docker-compose.test.yml up -d)
# and set DATABASE_URL to postgresql://postgres:postgres@postgres_test:5432/medspa_test
# or use .env.test when running pytest.
