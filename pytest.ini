[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -v --cov=app --cov-report=term-missing --cov-report=html
markers =
    unit: unit tests (service/model layer)
    integration: integration tests (API + test database)
